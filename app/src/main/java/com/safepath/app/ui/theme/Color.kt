package com.safepath.app.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Brand Colors ─────────────────────────────────────────────────────────────
val PurplePrimary    = Color(0xFF8B5CF6)   // Vibrant purple
val PurpleLight      = Color(0xFFA78BFA)
val PurpleDark       = Color(0xFF6D28D9)
val PinkAccent       = Color(0xFFEC4899)   // Hot pink
val PinkLight        = Color(0xFFF472B6)

// ─── Backgrounds ─────────────────────────────────────────────────────────────
val BackgroundDark   = Color(0xFF0A0A1A)   // Deep space black
val SurfaceDark      = Color(0xFF13132B)   // Card background
val SurfaceVariant   = Color(0xFF1E1E40)   // Elevated surface
val GlassWhite       = Color(0x1AFFFFFF)   // Glass morphism overlay

// ─── Safety Colors ────────────────────────────────────────────────────────────
val SafeGreen        = Color(0xFF00E676)   // Emerald safe
val SafeGreenDim     = Color(0xFF00E6761A)
val ModerateAmber    = Color(0xFFFFB300)   // Warning amber
val ModerateAmberDim = Color(0xFFFFB3001A)
val RiskyRed         = Color(0xFFFF1744)   // Danger red
val RiskyRedDim      = Color(0xFFFF17441A)

// ─── SOS ──────────────────────────────────────────────────────────────────────
val SOSRed           = Color(0xFFFF0040)
val SOSRedLight      = Color(0xFFFF4070)

// ─── Text ─────────────────────────────────────────────────────────────────────
val TextPrimary      = Color(0xFFFFFFFF)
val TextSecondary    = Color(0xFFB4B4CC)
val TextMuted        = Color(0xFF6B6B8A)

// ─── Misc ─────────────────────────────────────────────────────────────────────
val DividerColor     = Color(0x1AFFFFFF)
val IconTint         = Color(0xFFD4D4F0)
